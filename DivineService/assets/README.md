Drop the brand logo here as **divine_logo.png** (PNG, ~360px wide, transparent
background, navy wordmark). service_email.py embeds it inline in welcome emails
via a CID attachment. Override the path with the DIVINE_LOGO_PATH env var.
